# API keys and secrets — NEVER commit these
analyser/.env
analyser/credentials.json
analyser/token.json
analyser/.wwebjs_auth/
analyser/wa_messages.json
analyser/node_modules/

# ai_scores_log is local only — ai_scores.js IS committed (needed by GitHub Pages)
analyser/ai_scores_log.json
